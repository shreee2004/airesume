# For local dev (npm run dev) you can leave this empty — Vite proxies
# /api/* to localhost:8000 (see vite.config.ts).
#
# For production builds (deployed on Vercel), set this to your Render
# backend URL, e.g.:
# VITE_API_BASE_URL=https://airesume-backend.onrender.com
VITE_API_BASE_URL=
